/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { WaiterService } from "../waiter.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { WaiterCreateInput } from "./WaiterCreateInput";
import { Waiter } from "./Waiter";
import { WaiterFindManyArgs } from "./WaiterFindManyArgs";
import { WaiterWhereUniqueInput } from "./WaiterWhereUniqueInput";
import { WaiterUpdateInput } from "./WaiterUpdateInput";
import { TablesFindManyArgs } from "../../tables/base/TablesFindManyArgs";
import { Tables } from "../../tables/base/Tables";
import { TablesWhereUniqueInput } from "../../tables/base/TablesWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class WaiterControllerBase {
  constructor(
    protected readonly service: WaiterService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Waiter })
  @nestAccessControl.UseRoles({
    resource: "Waiter",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createWaiter(@common.Body() data: WaiterCreateInput): Promise<Waiter> {
    return await this.service.createWaiter({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        email: true,
        phoneNumber: true,
        restaurant: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Waiter] })
  @ApiNestedQuery(WaiterFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Waiter",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async waiters(@common.Req() request: Request): Promise<Waiter[]> {
    const args = plainToClass(WaiterFindManyArgs, request.query);
    return this.service.waiters({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        email: true,
        phoneNumber: true,
        restaurant: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Waiter })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Waiter",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async waiter(
    @common.Param() params: WaiterWhereUniqueInput
  ): Promise<Waiter | null> {
    const result = await this.service.waiter({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        email: true,
        phoneNumber: true,
        restaurant: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Waiter })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Waiter",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateWaiter(
    @common.Param() params: WaiterWhereUniqueInput,
    @common.Body() data: WaiterUpdateInput
  ): Promise<Waiter | null> {
    try {
      return await this.service.updateWaiter({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          email: true,
          phoneNumber: true,
          restaurant: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Waiter })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Waiter",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteWaiter(
    @common.Param() params: WaiterWhereUniqueInput
  ): Promise<Waiter | null> {
    try {
      return await this.service.deleteWaiter({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          email: true,
          phoneNumber: true,
          restaurant: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/tablesItems")
  @ApiNestedQuery(TablesFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Tables",
    action: "read",
    possession: "any",
  })
  async findTablesItems(
    @common.Req() request: Request,
    @common.Param() params: WaiterWhereUniqueInput
  ): Promise<Tables[]> {
    const query = plainToClass(TablesFindManyArgs, request.query);
    const results = await this.service.findTablesItems(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        tableNumber: true,
        status: true,

        waiter: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/tablesItems")
  @nestAccessControl.UseRoles({
    resource: "Waiter",
    action: "update",
    possession: "any",
  })
  async connectTablesItems(
    @common.Param() params: WaiterWhereUniqueInput,
    @common.Body() body: TablesWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tablesItems: {
        connect: body,
      },
    };
    await this.service.updateWaiter({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/tablesItems")
  @nestAccessControl.UseRoles({
    resource: "Waiter",
    action: "update",
    possession: "any",
  })
  async updateTablesItems(
    @common.Param() params: WaiterWhereUniqueInput,
    @common.Body() body: TablesWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tablesItems: {
        set: body,
      },
    };
    await this.service.updateWaiter({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/tablesItems")
  @nestAccessControl.UseRoles({
    resource: "Waiter",
    action: "update",
    possession: "any",
  })
  async disconnectTablesItems(
    @common.Param() params: WaiterWhereUniqueInput,
    @common.Body() body: TablesWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      tablesItems: {
        disconnect: body,
      },
    };
    await this.service.updateWaiter({
      where: params,
      data,
      select: { id: true },
    });
  }
}
